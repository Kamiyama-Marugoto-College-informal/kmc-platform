# Contributing to kmc-platform

## 目次

- [開発環境のセットアップ](#開発環境のセットアップ)
- [ブランチ戦略](#ブランチ戦略)
- [コミットメッセージ規則](#コミットメッセージ規則)
- [Issue の投げ方](#issue-の投げ方)
- [Pull Request の投げ方](#pull-request-の投げ方)
- [コードスタイル](#コードスタイル)
- [レビュープロセス](#レビュープロセス)

---

## 開発環境のセットアップ

### 必要なもの

| ツール  | 推奨バージョン |
| ------- | -------------- |
| Node.js | 22.x 以上      |
| npm     | 10.x 以上      |

### 手順

```sh
# 1. リポジトリをフォーク後、クローン
git clone https://github.com/<your-username>/kmc-platform.git
cd kmc-platform

# 2. 依存パッケージのインストール
npm install

# 3. 開発サーバーの起動
npm run dev
```

### 利用可能なスクリプト

| コマンド          | 説明                              |
| ----------------- | --------------------------------- |
| `npm run dev`     | 開発サーバーを起動（HMR 有効）    |
| `npm run build`   | 型チェック + プロダクションビルド |
| `npm run lint`    | ESLint によるコード検査           |
| `npm run preview` | プロダクションビルドのプレビュー  |

---

## ブランチ戦略

| ブランチ名        | 用途                                   |
| ----------------- | -------------------------------------- |
| `main`            | 本番リリース済みコード。直接 push 禁止 |
| `develop`         | 開発の統合ブランチ。PR のマージ先      |
| `feature/<name>`  | 新機能の開発                           |
| `fix/<name>`      | バグ修正                               |
| `docs/<name>`     | ドキュメントのみの変更                 |
| `refactor/<name>` | 機能変更を伴わないリファクタリング     |
| `chore/<name>`    | ビルド設定・依存関係の更新など         |

### ブランチ命名例

- issueがある場合は、issue idに合わせる。

```
feature/user-authentication
fix/login-redirect-loop
docs/update-contributing-guide
```

---

## コミットメッセージ規則

### type 一覧

| type       | 用途                                       |
| ---------- | ------------------------------------------ |
| `feat`     | 新機能                                     |
| `fix`      | バグ修正                                   |
| `docs`     | ドキュメントのみの変更                     |
| `style`    | フォーマット変更（ロジックの変更なし）     |
| `refactor` | リファクタリング（機能変更・バグ修正なし） |
| `test`     | テストの追加・修正                         |
| `chore`    | ビルド設定・依存関係の更新                 |
| `perf`     | パフォーマンス改善                         |
| `revert`   | 以前のコミットの取り消し                   |

---

## Issue の投げ方

Issue を起票する前に、**既存の Issue と重複していないか検索してください**。

### バグ報告

「🐛 Bug Report」テンプレートを使用してください。

記載が必要な情報：

- **再現手順** — 問題を再現するための具体的なステップ
- **期待する動作** — 本来どう動くべきか
- **実際の動作** — 何が起きているか
- **環境** — OS・ブラウザ・Node.js バージョンなど
- **スクリーンショット / エラーログ** — あれば添付する

### 機能要望

「✨ Feature Request」テンプレートを使用してください。

記載が必要な情報：

- **背景・課題** — なぜその機能が必要か
- **提案する解決策** — どのような実装・UX を想定しているか
- **代替案** — 他に検討した方法があれば

### Issue を起票するときの心がけ

- タイトルは一文で問題・要望を表す（例：`ログイン画面でメールアドレスのバリデーションが機能しない`）
- 再現しない・情報が足りない Issue はクローズされる場合があります
- 作業開始前に Issue でアサインを確認してください（重複作業防止）

---

## Pull Request の投げ方

### PR を出す前のチェック

- `develop` ブランチから最新の変更を取り込んでいること
- `npm run build` がエラーなく通ること
- `npm run lint` がエラーなく通ること
- 既存の動作を壊していないこと

### PR の作成手順

```sh
# 1. develop から作業ブランチを切る
git switch develop
git pull origin develop
git switch -c feature/your-feature-name

# 2. 変更を加えてコミット
git add .
git commit -m "feat(scope): わかりやすい説明"

# 3. push
git push origin feature/your-feature-name
```

その後、GitHub 上で develop ブランチに向けて PR を作成してください。

### PR テンプレート

PR を作成すると自動でテンプレートが読み込まれます。
**すべての項目に記入してから** レビューリクエストを送ってください。

### PR の粒度

- 1 PR = 1 つの目的に絞る
- 500 行を超える場合は分割を検討する
- `draft` PR で早めにフィードバックをもらうことを推奨します

### PR タイトルの書き方

コミットメッセージと同じ Conventional Commits 形式を使います。

```
feat(auth): Google OAuth によるログイン機能を追加
fix(ui): モバイル表示のナビゲーション崩れを修正
```

---

## コードスタイル

### TypeScript

- `any` は使用禁止。型が不明な場合は `unknown` を使う
- 根拠のない `as` キャストを避ける
- 関数の引数・戻り値には必ず型を付ける
- `interface` より `type` を優先する（ユニオン型などの柔軟性のため）

### React

- コンポーネントは関数コンポーネントで統一
- Props の型定義はコンポーネントの直上に置く
- ファイル名はコンポーネント名と一致させる（PascalCase）
- フックは `src/hooks/` に配置する

### ファイル構成

```
src/
├── components/    # 再利用可能な UI コンポーネント
├── hooks/         # カスタムフック
├── pages/         # ページコンポーネント
├── types/         # 型定義
└── utils/         # 純粋関数・ユーティリティ
```

### ESLint

`npm run lint` を必ず実行してください。
設定（`eslint.config.js`）は変更しないでください。変更が必要な場合は先に Issue で議論してください。

---

## レビュープロセス

### レビューの分類

| ラベル     | 意味                                 |
| ---------- | ------------------------------------ |
| **Must**   | マージ前に必ず修正が必要             |
| **Should** | 修正を強く推奨するが、議論の余地あり |
| **Nit**    | 細かい好みの問題。対応は任意         |

### マージまでの流れ

1. PR を提出する
2. CI（lint / build）がグリーンになる
3. レビュアーから最低 1 件の Approve を受ける
4. **Must** の指摘がすべて解消されている
5. `develop` へ Squash Merge する

### 返信の目安

- レビューリクエストから 3 営業日以内に初回レビューを行います
- 修正後は再レビューを依頼してください（コメントに `@mention` する）
